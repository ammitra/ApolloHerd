type: ApolloDevice

libraries:
  - libApolloHerd.so

devices:
  - id: ApolloSM_device
    class: swatch::apolloherd::ApolloDevice
    uri: blah:///dev/fileA
    addressTable: /path/to/addressTableA.xml